#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    int count = 0;
    
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;
    ++count;

    puts("Jay Laddoo Gopal\n");

    return EXIT_SUCCESS;
}